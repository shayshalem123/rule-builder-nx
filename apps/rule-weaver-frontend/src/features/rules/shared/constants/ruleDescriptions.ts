/**
 * Constants for rule type descriptions to maintain consistency across components
 */
export const RULE_DESCRIPTIONS = {
  AND: {
    FULL: "All conditions must match",
  },
  OR: {
    FULL: "At least one condition must match",
  },
};

export default RULE_DESCRIPTIONS; 